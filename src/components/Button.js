import styled from 'styled-components';
import tw from 'twin.macro';

export const Button = styled.button`
  ${tw`bg-primary hover:bg-primary-dark text-white py-2 px-4 rounded focus:outline-none focus:shadow-outline w-full`}
`;
