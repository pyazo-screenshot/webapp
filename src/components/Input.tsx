import styled from 'styled-components';
import tw from 'twin.macro';

export const Input = styled.input`
  ${tw`bg-background-dark appearance-none border border-primary rounded w-full py-4 px-6 text-white
   leading-tight focus:outline-none focus:bg-background-dark focus:border-primary
   `}
`;
